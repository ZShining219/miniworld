# MiniWorld Agent：项目目标与 Demo 执行契约

> 状态：v0.7 Demo 已冻结；v0.8 岗位雷达本地呈现模块已完成验收，真实岗位地图接入仍暂缓
>
> 版本：v0.8
>
> 日期：2026-08-19
> 权威性：本文件是项目最高优先级的产品与边界契约

## 1. 文档目的与防偏移规则

本项目要为本机唯一用户搭建一个本地优先的个人 Agent，并提供前端看板。本文件固定用户意图、Demo 范围、权限边界、隐私规则和验收标准。

任何技术选型、开源基座、Agent 框架或实现便利都不能改变本文件中的核心意图。发生冲突时按以下规则处理：

1. 用户明确表达的意图优先于技术方案；
2. 本文件优先于 `goal/` 中的技术文档；
3. 技术方案必须适配目标，不能让目标迁就框架；
4. 岗位搜索、个人档案/简历、每日工作沉淀必须保持三个独立闭环；
5. 精确住址隐私和外部交互确认属于不可静默放宽的强约束；
6. 如需改变核心目标或强约束，必须先获得用户明确确认，并记录新决策与被替代决策。

本文中的“必须”表示 Demo 冻结前不可省略的验收条件；“允许”不等于“必须”；“当前不锁定”表示实现可保留配置项或占位，不得由开发者擅自替用户决定；“后续方向”不是当前实现授权。验收项只有在 `goal/implementation-log.md` 中存在可复现证据时才能勾选，代码存在、测试替身运行或页面静态展示本身不等于目标完成。

配套文档位于 [`goal/`](goal/README.md)，负责解释架构、调研、计划、决策和实施记录，但不拥有改写本文件意图的权力。原始对话到本文件和实现证据的逐条映射见 [`goal/intent-audit.md`](goal/intent-audit.md)。

## 2. 项目意图

一句话定义：**MiniWorld Agent 是一个只服务本机唯一用户的个人求职与成长工作台，用本地 LangGraph 工作流把岗位发现、个人档案/简历和每日工作沉淀组织为三个可追溯、可恢复、权限明确的独立闭环。**

项目长期服务于三个方向：

1. 围绕用户住址持续查找、整理工作与实习信息；
2. 沉淀用户个人信息，并支持持续更新个人档案和简历；
3. 沉淀用户的每日工作，形成日报和周报。

当前阶段以完成一个本机可运行、可验证、可继续扩展的 Demo 为目标，不追求一次性完成完整求职平台。

Demo 的完成不以“已经安装 LangGraph”或“页面能够打开”为准，而以三个闭环都能从真实的前端/API 操作入口产生可追溯的本地结果、关键隐私规则有自动化测试、冷启动操作可复现为准。默认 `demo` 模式可以使用明确标记的确定性适配器；互联网岗位能力必须另外提供一次 `live` 只读验证，不得用固定数据替代或冒充。截至 v0.7，本地三闭环和 Lever 公开 GET 的 Live 验证均已有可复现证据。真实远端模型调用属于用户授权后的可选增强，不是用户要求的 Demo 完成门。

### 2.1 用户意图锁定矩阵

| 主题 | 当前必须保持的解释 | 当前不做或不锁定 |
| --- | --- | --- |
| 地址含义 | 用户输入的是本机唯一用户的居住地址；精确值只在本地参与距离计算 | 不把精确地址作为外部搜索参数 |
| 岗位能力 | 从公开互联网来源查找、整理、去重并更新岗位，展示到住址的直线距离 | 不做匹配评分、投递跟踪或自动投递 |
| 地址对外策略 | 本地保存精确地址/坐标，对外查询只使用用户配置的附近变动地标 | 最终地标选择和轮换策略暂不替用户决定 |
| 档案与简历 | 新材料导入后及时形成可追溯事实和版本化简历草稿 | 岗位发现不触发针对岗位的简历改写 |
| 输入能力 | 采用可扩展的多来源、多格式输入；Demo 优先手动导入本地文件、公开 GitHub 信息和 GPT 对话材料 | 图片/OCR、音频、在线账号直连和自动抓取不作为本轮完成条件 |
| 工作沉淀 | 用户主动记录每日工作，生成可追溯的日报和周报 | 不自动监控整台电脑或任意工作来源 |
| 自动化权限 | 允许定时公开读取和限定的本地数据更新 | 投递、消息、上传、登录授权、平台修改和其他外部写入必须确认 |
| 部署取向 | 单机、单用户、本地优先，允许受控远端 AI 推理 | 不做多用户、云 SaaS 或公网暴露 |
| 岗位雷达呈现 | 本机可调大小的悬浮窗；用户位置固定在街道地图中心；有坐标的岗位显示为黄色闪光点 | 岗位数据接入、匹配评分、投递跟踪与路线时间不属于本阶段 |

## 3. Demo 核心闭环

### 3.1 岗位发现闭环

1. 用户只在本地配置精确住址、精确坐标和多个附近地标；
2. Agent 使用附近地标而不是精确住址访问限定的公开互联网来源；
3. Agent 把职位信息标准化、去重并保存到本地；
4. Agent 获取或解析职位工作地点坐标；
5. Agent 在本地计算工作地点到精确住址坐标的直线距离；
6. 前端展示职位、来源、更新时间、距离和无法计算时的明确原因。

v0.7 Demo 只要求岗位信息包含可靠的直线距离结果并可查看。v0.8 在不改变岗位闭环的前提下新增独立呈现层：本机悬浮窗使用街道级地图，以用户本地位置为固定中心，以黄色闪光点显示有可靠坐标的岗位；地点未解析的岗位保留在列表和计数中，不伪造地图位置。

### 3.2 个人档案与简历闭环

1. 用户主动导入本地文件、GitHub 信息或 GPT 对话导出材料；
2. 系统先在本地解析、分类并记录来源；
3. 需要语义理解时，通过受控的模型网关调用用户配置的远端 AI；
4. 系统将返回结果校验为结构化事实，并保存证据和变更记录；
5. 系统更新本地个人档案和简历内容草稿；
6. 职位搜索模块不能直接触发或修改个人档案与简历。

“实时更新”指新材料完成导入和处理后，个人档案与简历草稿能够及时反映变化；不代表发现职位后自动针对职位改写简历。

### 3.3 每日工作沉淀闭环

1. 用户主动记录每日工作内容；
2. 系统把记录保存在本地；
3. 系统可按日或按周调用受控模型生成日报、周报；
4. 报告保存在本地，并能追溯到原始工作记录；
5. 日报或周报中的内容不会自动进入个人档案，转入前需要用户确认。

### 3.4 用户操作主路径

Demo 面向用户的最短操作路径为：

1. 首次打开看板，完成本地位置、附近地标和可选模型 Provider 配置；
2. 在 `Jobs` 手动运行一次岗位发现，查看标准化岗位、来源、采集状态和直线距离；
3. 在 `Profile` 手动导入一份材料，查看可追溯事实和新的简历草稿版本；
4. 在 `Work` 记录当天工作，并生成一份日报或基于多日记录生成周报；
5. 在 `Agent Runs` 查看每次 Graph 的节点状态、失败原因、策略阻断和安全重试入口；
6. 定时岗位任务在启用后自动执行，但任何外部写入仍停在显式确认门前。

该路径描述的是 Demo 必须打通的用户体验，不锁定最终页面排版。

## 4. Demo 功能范围

### 4.1 职位查找与整理

Demo 包含：

- 配置一个或少量明确、允许访问的公开职位来源；
- 手动触发和定时执行职位查询；
- 提取职位名称、公司、工作地点、来源、原始链接、发布时间和采集时间等字段；
- 对相同来源 ID、相同链接或高可信组合键进行幂等去重；
- 在来源数据变化时更新限定的本地职位数据；
- 对来源限流、页面变化、字段缺失和解析失败进行记录；
- 在本地计算 Haversine 直线距离；
- 对距离状态标注“已计算”“地点待解析”或“计算失败”；
- 在前端查看职位、距离、来源和采集运行状态。

Demo 不要求覆盖全网。可靠完成一个公开来源的端到端流程，优先于来源数量。

### 4.2 个人信息与简历

Demo 包含：

- 建立统一的材料入口协议，使文件、公开 URL 和对话导出都能进入同一套来源、授权和证据链；
- 可扩展的材料导入入口，保存来源、导入时间、内容哈希和处理状态；
- 至少支持一种本地文档格式；
- 支持用户主动提供公开 GitHub 仓库或个人主页信息；
- 支持手动导入 GPT/ChatGPT/Codex 对话文本或导出文件；
- 从材料中提取经历、项目、技能、成果等结构化事实；
- 每条事实保留来源证据、置信状态和更新时间；
- 根据已确认或高可信事实更新本地简历草稿；
- 简历草稿采用可导出、可演进的结构化数据，参考 JSON Resume，但当前不锁定最终版式。

系统不会直接读取用户的 GPT、ChatGPT 或 Codex 账号历史，只处理用户主动导入的材料。

### 4.3 每日工作沉淀

Demo 只实现最小闭环：

- 提供每日工作记录入口；
- 支持按日期保存和修改用户主动提交的内容；
- 根据记录生成简单日报和周报；
- 报告保留生成时间、模型信息和来源记录范围。

自动读取全部电脑活动、日历、Git 提交或其他工作来源暂不进入 Demo。

## 5. 已选技术基线

技术基线用于提升可实现性和工程认可度，不构成新的产品目标。

### 5.1 本地运行形态

- 使用 Docker Compose 在本机运行；
- 前端、API、后台 Worker 和 PostgreSQL 均为本地容器；
- 默认只绑定本机回环地址，不对局域网或公网开放；
- 使用现有 `docker-compose` 命令兼容本机环境；
- README 或仓库脚本提供一条统一冷启动命令，并显式处理本机旧式 Compose 与 BuildKit 的兼容差异；
- Apple Silicon/ARM64 是首个必须验证的平台。

### 5.2 应用基座

- 以 FastAPI 官方 `full-stack-fastapi-template` 作为工程基座；
- 保留 FastAPI、React、TypeScript、Vite、Tailwind/shadcn、SQLModel、PostgreSQL、Pytest 和 Playwright 等成熟结构；
- 删除或禁用 Demo 不需要的多用户、邮件找回、云部署和公开注册能力；
- 不继承上游仓库的 `.git` 历史、Agent 指令或与本项目无关的云配置；
- 导入时固定上游 commit，并记录来源、许可证、导入范围和本地修改。

### 5.3 Agent 编排

- 使用 LangGraph OSS Python 包作为本地 Agent 编排核心；
- LangGraph 作为普通 Python 依赖运行在本机 `api`/`worker` 容器内，不要求 LangGraph Cloud、LangSmith 或其他托管运行时；
- 三个核心闭环分别实现为独立 Graph，共享基础设施但不共享隐式业务状态；
- 使用 PostgreSQL checkpoint 保存图运行状态，支持失败恢复和审计；
- 需要用户确认的未来外部行为使用明确的 interrupt/approval 节点；
- LangGraph 只负责编排，不拥有绕过权限策略、读取任意本地文件或直接外发数据的能力。

选择 LangGraph 不只是为了使用前沿名词，而是为了在代码中展示状态图、持久化执行、失败恢复、结构化模型输出、human-in-the-loop 和策略节点等可验证工程能力。FastAPI、React/TypeScript、PostgreSQL、Docker、Pytest 和 Playwright 共同构成更完整、也更接近实际岗位要求的项目证据。

### 5.4 数据与辅助能力

- PostgreSQL 保存本地结构化业务数据和 Agent 状态；
- JobSpy 作为可替换的尝试性职位来源适配器，不是不可替换的领域依赖；
- Live 来源优先级为“公司公开 ATS/Job Board API（首个候选为 Lever Postings API）→ JobSpy 最善努力适配器”；禁止通过登录绕过、验证码绕过或高频爬取取得数据；
- Microsoft MarkItDown 负责在本地将文档转换为适合分析的文本；
- JSON Resume schema 作为简历结构参考，不直接绑定某个简历前端项目；
- Demo 的距离计算使用本地 Haversine 算法，不依赖外部路线服务；
- 定时任务由独立 Worker 中的 APScheduler 触发并调用相应 LangGraph，不在前端或 API 请求进程内执行。

### 5.5 可选远端 AI 注入

- 后端提供统一 `ModelProvider` 接口，业务 Graph 不直接绑定单一厂商；
- 首期支持一个 OpenAI/GPT 类远端 Provider，使用 Responses API 的 Structured Outputs/Pydantic schema 返回结构化结果，并为其他兼容 Provider 预留适配；
- 本地 Agent 负责材料解析、字段筛选、权限判断、结果校验和持久化；
- 远端模型只处理当前任务所需的最小文本片段，不获得整个本地数据库；
- 精确住址、精确坐标、模型密钥和其他明确禁止字段必须在出站策略层被阻断；
- 远端模型返回内容视为不可信输入，必须通过结构化 schema 校验后才能写入业务表；
- API 密钥仅通过本地 secret/environment 配置注入，不进入数据库、前端、日志或 Git；
- 模型调用审计只保存 Provider、模型、时间、用途、数据分类和内容哈希，默认不保存完整敏感提示词。
- Demo 在没有远端模型、API Key 或个人材料外发授权时仍必须完整运行；真实远端调用不作为当前 Demo 冻结条件。

### 5.6 两种可运行模式

Demo 必须明确区分两种模式，避免把外部服务不稳定误判为本地系统不可运行：

- `demo` 模式：默认启用固定岗位数据和确定性模型替身，不需要 API 密钥或互联网；用于冷启动、测试、演示三个闭环和验证隐私边界；
- `live` 岗位模式：用户主动配置允许的公开职位来源后启用，用于验证真实公开只读来源；
- 远端模型模式：与岗位模式独立配置，只有用户确认 Provider、模型和数据类别后才启用。真实结构化调用是可选扩展验证，不影响本地 Demo 可用性。

界面和运行记录必须明确标注当前模式、数据来源和模型类型。替身产生的结果不得冒充实时互联网信息或真实模型推理。

## 6. 模块隔离原则

三个模块具有独立的数据入口、Graph、运行记录和写入权限：

- 职位搜索不能读取精确住址文本，只能在本地距离节点使用精确坐标；
- 职位搜索不能读取或修改个人档案、简历和工作记录；
- 个人档案 Graph 只处理用户主动导入或明确确认的材料；
- 工作报告 Graph 只读取选定日期范围内的工作记录；
- 任何跨模块数据提升都必须通过显式、可审计的用户确认动作；
- 数据库外键或共享表不能被用来建立未声明的隐式耦合。

## 7. 住址与隐私强约束

用户精确住址属于高敏感数据：

1. 精确住址及精确坐标只保存在本地；
2. 精确住址不得发送给招聘网站、搜索服务、地图服务、远端模型或其他外部服务；
3. 用户在本地直接填写或校正精确坐标，Demo 不把精确门牌地址交给外部地理编码服务；
4. 外部查询使用用户配置的附近地标，不使用精确门牌地址；
5. 系统支持在多个附近地标之间轮换，记录本次使用的地标 ID，但不把地标与精确住址建立外部关联；
6. 职位地点坐标可以从公开信息或外部地理编码服务获取；
7. 职位到精确住址的最终距离只在本地计算；
8. Demo 结果必须标注为直线距离，不得描述为驾车、公交、骑行或步行距离；
9. API 响应、日志、报错、遥测、模型请求和前端调试信息不得泄露精确住址；
10. 精确地址处理与附近地标外部查询必须是两个独立、可审计步骤。

### 7.1 数据分级与默认处理

| 数据级别 | 例子 | 默认存储 | 是否允许外发 |
| --- | --- | --- | --- |
| `restricted_local` | 精确住址、家庭坐标、API 密钥、未选择文件 | 仅本机受控存储；密钥不入数据库 | 禁止 |
| `personal_authorized` | 用户选择的简历片段、工作记录、GPT 对话片段 | 本机；保留来源和授权状态 | 仅在当前任务、Provider 和数据类别获得授权且完成最小化后允许 |
| `public_query` | 附近地标、职位关键词、公开 GitHub URL | 本机配置或运行记录 | 可向白名单公开来源读取查询 |
| `public_result` | 公开职位、公开仓库元数据 | 本机业务数据库 | 可读取；外部写入仍需确认 |

数据分级必须发生在任何出站调用之前。无法确定级别时按更严格级别处理并暂停调用，不能默认放行。

## 8. Agent 权限边界

Agent 可以在用户预先限定的范围内：

- 定时访问已配置的公开信息来源；
- 自动整理、去重并更新限定的本地职位数据；
- 自动记录执行状态、失败原因和更新时间；
- 基于用户选择的本地记录生成本地产物；
- 调用已配置远端模型完成用户已授权的数据类别和任务。

以下行为必须在实际执行前获得用户明确确认：

- 提交职位申请；
- 向招聘方或其他外部对象发送消息；
- 上传简历、个人材料或本地文件；
- 首次允许某类个人材料发送给某个远端模型 Provider；
- 登录或授权第三方账号；
- 修改外部平台上的任何内容；
- 推送可能包含个人数据的代码、数据或文档到公开 Git 仓库；
- 执行超出已限定来源、数据类别和用途的外部操作。

默认原则是：允许有限的自动读取和本地更新，不允许未经确认的外部写入或个人信息外发。

执行时按下表判定，不能因为某个动作由 Agent、定时任务或用户点击间接触发而改变权限级别：

| 动作类型 | 例子 | 默认处理 |
| --- | --- | --- |
| 公开只读 | 使用附近地标查询白名单职位来源 | 配置范围内可手动或定时执行，并记录来源与运行 |
| 本地写入 | 保存职位、事实草稿、日报周报和运行状态 | 可自动执行，但只能写入当前 Graph 被授权的表 |
| 个人材料外发推理 | 将已选择、已最小化的材料片段发给远端模型 | 首次 Provider × 数据类别授权后才允许，并保留不含敏感正文的审计 |
| 外部写入或账号操作 | 投递、发消息、上传、登录授权、修改外部平台 | 每次实际执行前显式确认；Demo 不实现真正执行 |

## 9. 当前非目标

以下内容不作为本轮 Demo 完成条件：

- 岗位匹配评分与推荐排序；
- 自动生成针对特定岗位的简历或求职信；
- 投递、面试和 Offer 状态跟踪；
- 自动投递或自动联系招聘方；
- 覆盖所有招聘网站；
- 绕过验证码、登录限制、反爬规则或网站安全措施；
- 精确的驾车、公交、骑行或步行距离与通勤时间；
- 岗位雷达以外的最终前端布局与完整视觉系统；
- 确定完整岗位偏好体系；
- 确定最终简历版式；
- 自动采集全部电脑活动；
- 直接读取用户在线 AI 账号历史；
- 多用户、团队协作、云端 SaaS 部署；
- Kubernetes、微服务拆分或复杂多 Agent 自治系统。

## 10. Demo 验收标准

验收证据分为三层：`demo` 证据证明无密钥、无网络时的可复现性；`live` 证据证明公开互联网只读适配器真实可用；隐私证据证明两种模式都不能泄漏受限数据。每一项必须记录命令、测试、API 响应或人工操作结果以及对应日期，不能只写“已实现”。

v0.7 勾选只表示 2026-08-18 已在当前工作树上取得证据，对应命令、数字和 Git 状态记录在 `goal/implementation-log.md` 与 `PROGRESS.md`。本节全部必选项已经通过；后续可选能力不得在 README、演示或作品集中冒充已完成。

### 10.1 基础运行

- [x] README 提供的单条冷启动命令能在 ARM64 本机完成构建并启动前端、API、Worker 和 PostgreSQL；
- [x] 对宿主机发布的端口只绑定 `127.0.0.1`；容器内部服务不对局域网或公网直接发布；
- [x] 数据在容器重启后仍保存在本地卷；
- [x] 前端能展示三个独立模块及 Agent 运行状态。

### 10.2 岗位闭环

- [x] 能在本地保存精确坐标，并配置至少两个附近地标；
- [x] 适配器入参、策略测试、公开 API、前端 bundle 和普通容器日志中不包含精确住址或精确坐标；
- [x] 能从至少一个公开来源完成一次端到端职位采集；
- [x] 职位记录保留原始链接、来源和采集时间；
- [x] 重复运行不会无控制生成重复职位；
- [x] 能计算并展示有效职位的 Haversine 直线距离；
- [x] 地点无法解析时能展示明确状态和原因；
- [x] 能手动触发，也能完成一次由 Worker 真正执行的定时触发；
- [x] 失败运行可查看原因，重试不会破坏已有数据。

### 10.3 个人档案与简历闭环

- [x] 能手动导入至少一种本地文档；
- [x] 能手动提供公开 GitHub 信息；
- [x] 能手动导入 GPT 对话文本或导出文件；
- [x] 档案 Graph 通过统一 Provider 与出站策略运行；无远端授权时可用明确标记的确定性 Provider 完成闭环，配置错误时不会静默外发；
- [x] 模型输出经过结构化校验，失败时不会写入正式事实；
- [x] 新事实能反映到本地个人档案和简历草稿；
- [x] 每条事实能追溯到原始材料；
- [x] 职位 Graph 不会直接修改个人档案或简历。

### 10.4 每日工作闭环

- [x] 能记录一条每日工作内容并生成日报；
- [x] 能根据多日记录生成周报；
- [x] 报告能追溯到使用的日期范围和原始记录；
- [x] 报告内容不会未经确认进入个人档案。

### 10.5 权限与安全闭环

- [x] API 密钥和本地敏感配置不进入 Git、前端 bundle 或普通日志；
- [x] 确定性 Provider 的模型调用和 Agent 运行具有不含敏感正文的审计记录；
- [x] 未配置或未授权远端 Provider 时，相关 Graph 明确暂停而不是静默外发；
- [x] Demo 不暴露投递、消息发送、资料上传或其他外部写入执行器；未来行为只预留本地 Approval 记录与决策 API。

### 10.6 工程复现与作品证明

- [x] 无 API 密钥、无真实个人资料时，`demo` 模式可完整演示三个闭环；
- [x] `live` 模式与 `demo` 模式在界面、配置和审计中可明确区分；
- [x] LangGraph 至少展示三个独立 Graph、持久化 checkpoint、失败状态和一次可恢复重试；
- [x] OpenAPI、数据库迁移、单元/集成测试和前端 E2E 测试可从 README 中按命令复现；
- [x] 第三方基座 commit、许可证、依赖版本和本项目修改边界可追溯；
- [x] 项目可用一条不含真实个人数据的演示路径说明其业务价值和技术价值。
- [x] 公开作品说明能区分已实现能力、确定性替身、真实联网证据与后续规划，不以框架名称代替工程证据。

### 10.7 非阻塞的可选 Live Provider 验证

用户醒后如选择启用远端模型，再单独验证：指定 Provider 与模型、明确允许外发的数据类别、使用最小化且明确选择的材料完成一次真实 Structured Outputs 调用，并复核审计与撤销方式。该验证不会替代本节已完成的本地 Demo 证据，也不是创建可运行 Demo 的前置条件。

### 10.8 v0.8 岗位雷达呈现验收（已通过）

- [x] macOS 本机可打开独立岗位雷达悬浮窗，并可拖动、缩放、置顶和关闭；
- [x] 悬浮窗使用本地街道底图，运行时不向外部地图或瓦片服务发送精确住址、精确坐标或住所视口；
- [x] 用户位置始终位于地图中心且不显示精确地址文本或坐标数值；
- [x] 有可靠坐标的岗位以黄色闪光点显示，点击后只展示岗位摘要、距离和本地已有来源信息；
- [x] 地点未解析的岗位不进入地图坐标层，同时保留明确的未解析计数与列表入口；
- [x] 320×320 到 900×700 的窗口尺寸下，地图、中心点、信号点和控制条均可用；
- [x] 离线、无岗位和地图包缺失均有明确状态，地图资源、许可证归属和本地存储位置可审计。

## 11. 需求到实现的追踪规则

每个实现任务、提交或测试至少关联一个需求编号，避免代码增长但目标不可验证：

| 需求编号 | 目标 | 主要实现位置 | 核心验收 |
| --- | --- | --- | --- |
| `REQ-JOB` | 岗位查找、整理与本地距离 | `JobDiscoveryGraph`、职位适配器、Jobs 页面 | 10.2 |
| `REQ-PROFILE` | 材料导入、事实与简历草稿 | `ProfileIngestionGraph`、导入/档案页面 | 10.3 |
| `REQ-WORK` | 每日记录、日报与周报 | `WorkReportGraph`、Work 页面 | 10.4 |
| `REQ-PRIVACY` | 精确住址不外发、最小化模型输入 | 出站策略、位置存储、审计与隐私测试 | 10.5 |
| `REQ-RUNTIME` | 本地运行、恢复与可观察 | Compose、PostgreSQL checkpoint、Worker、Agent Runs | 10.1、10.6 |
| `REQ-RADAR` | 本地街道地图、悬浮窗与岗位信号呈现 | Radar 场景 API、MapLibre/PMTiles、Tauri 窗口 | 10.8 |

实现日志必须记录关联需求、实际证据和未通过项；计划完成百分比不能替代验收证据。

## 12. 文档与实现职责

- `goal.md`：最高优先级的用户意图、强约束和验收标准；
- `goal/architecture.md`：容器、Graph、接口、数据模型和失败处理；
- `goal/research.md`：开源项目调研、选择与排除理由；
- `goal/plan.md`：可按阶段执行的实现计划和 Git 策略；
- `goal/decisions.md`：人类可读的决策记录和变更规则；
- `goal/implementation-log.md`：按时间追加的实际实现与验证记录；
- `goal/intent-audit.md`：原始对话、Goal 条款、实现证据与非目标的逐项核对；
- `README.md`：实现开始后面向使用者说明安装、运行、功能边界和已知限制；
- `.ai/`：Codex 窗口间的运行状态、锁、事件和交接记录。

## 13. 后续方向

Demo 稳定后才评估：

1. 扩充可靠职位来源；
2. 引入岗位偏好和匹配评分；
3. 由用户选择公开城市/区域后替换虚构地图包，并接入真实本地岗位坐标；
4. 增加投递、面试和结果跟踪；
5. 支持针对岗位的简历版本与求职材料；
6. 扩展每日工作信息来源；
7. 在用户逐项授权下增加外部协作能力。
